{
  "scripts": {
    "cypress:open": "cypress run --headless --browser chrome --env allure=true"
  },
  "devDependencies": {
    "@cypress/xpath": "^2.0.3",
    "cypress": "^10.8.0",
    "cypress-xpath": "^2.0.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@shelex/cypress-allure-plugin": "^2.34.0",
    "allure-commandline": "^2.20.1",
    "dotenv": "^16.0.3"
  }
}
