{
  "scripts": {
    "cypress:open": "cypress open"
    "cypress:run": "cypress run --browser chrome --headless"
  },
  "devDependencies": {
    "cypress": "^10.8.0",
    "cypress-xpath": "^2.0.1"
  }
}
